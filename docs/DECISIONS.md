# Product and architecture decisions

## Record semantics

This log captures decisions agreed for List & Split as of 2026-07-26. **Accepted**
means the direction is agreed; it does not mean the behavior is implemented. Check
code, tests, migrations, and pull requests for implementation status.

Open questions are listed separately and must not be resolved implicitly in code.
When a decision changes, update this log and the affected product, architecture,
and data-model documents in the same pull request.

The active-list resolutions requested as Decisions 33a through 36a are recorded
below as `P-033a`, `P-034a`, `A-035a`, and `A-036a`.

## Accepted decisions

### Product

| ID | Decision | Consequence |
| --- | --- | --- |
| P-001 | The product name is **List & Split**. The Flutter package/project name is `list_and_split`. | Human-facing and code identifiers deliberately differ. |
| P-002 | The application combines active/shared lists, templates, community, optional Payment Control, and notifications. | These are product areas, not claims of implementation status. |
| P-003 | The former term **Internal Lists** is replaced by **Templates**. | New UI, code, and documentation use “Templates.” |
| P-004 | Active-list members can add, edit, complete, and delete items with quantities, optional units, and multiple assignees. Lists also have one optional General Note with explicit current-member `@mentions`. | Assignment authorization is fixed by P-048; General Note/mention behavior is fixed by P-049 and P-050. |
| P-005 | Only accepted friends can be invited to an active/shared list. | Friendship is a server-validated prerequisite for invitation creation. |
| P-006 | Templates contain reusable ordered items, can be private or public, and are organized by owners into personal categories. The initial template slice is private and has no groups or collaborative editing. | Visibility and personal organization are separate concerns; public/shared behavior requires later decisions and schema. |
| P-007 | Importing a template into an active list is a snapshot copy. Saving a public template or accepting a sent template creates an independent recipient-owned deep copy. | Source changes or deletion never mutate existing imports/copies. New identifiers and atomic/idempotent copy operations are required. |
| P-008 | Community relationships are mutual friendships, not followers. Block-aware discovery by unique username precedes sending, accepting, or declining friend requests. | Friendship has symmetric meaning even though requests are directional; discovery and requests do not precede basic blocking. |
| P-009 | Public templates appear on profiles, and the community feed shows recent public templates from friends. | Private templates are excluded. Feed v1 ordering, pagination, eligibility, and freshness are fixed by P-056; ranked or recommended discovery remains deferred. |
| P-010 | Public content must support basic blocking and reporting. | Phase 1 blocking precedes discovery; Public Template reporting/moderation is fixed by P-053 and P-054. |
| P-011 | Payment Control is available only when enabled inside an active list and is an expense ledger, not payment processing. Each enabled list has one currency. | No wallet/payment-provider integration; ledger access follows list membership. |
| P-012 | An expense has one payer and selected participants, with equal or exact custom splits. Settlements can be recorded between list-scoped financial participants. | Participant/share and settlement records require strict server validation. |
| P-013 | Money is represented only in integer minor units; authoritative balance and debt calculations run server-side and have unit tests. | Floating-point monetary values are prohibited at every boundary. |
| P-014 | The in-app notification centre is persistent. Friend requests, list invitations, and sent templates require Accept/Decline; assignments and mentions notify recipients. | Assignment notification behavior is fixed by P-048 and mention behavior by P-050; actionable state remains authoritative in its underlying aggregate and must be safe under retry/stale UI. |
| P-015 | The primary destinations are Lists, Templates, Community, and Profile. Notifications open from a bell, not a fifth tab. | The root navigation shell has exactly four primary destinations. |
| P-016 | English and Portuguese are supported UI languages, and the application remains ready for additional locales. | User-facing strings, semantics, and layout must be complete in both supported languages and must not assume either is permanent. |
| P-017 | Initial authentication uses email and password only. Email verification is mandatory before authenticated application access; sign-up, sign-in, sign-out, verification resend, forgotten-password, and password-recovery flows are required. New and replacement passwords require at least eight characters, and confirmation must match the unmodified password exactly. | Google, Apple, anonymous, magic-link-only, and other providers are excluded from the initial release. Authentication errors must not disclose account existence where that would be inappropriate. Passwords are not trimmed, lowercased, or subject to composition rules. |
| P-018 | A user chooses a username during onboarding after authentication and email verification. It is trimmed, lowercased, globally unique, matches `^[a-z][a-z0-9_]{2,23}$`, and becomes immutable after onboarding. Display name is separate, trimmed, non-unique, editable, and 1-50 characters. | PostgreSQL enforces canonicalization, validation, uniqueness, and immutability. Email remains private and is never copied into public profile data. |
| P-019 | The initial profile slice is owner-only: a user can read and update only their own approved profile fields. Cross-user discovery waits for a block-aware friendship slice. | A permissive interim profile-read policy is prohibited. Avatar editing remains accepted future behavior, outside the initial profile slice. |
| P-020 | Basic user blocking is sequenced in Phase 1 before username discovery and friend requests. | The blocking/discovery slice is independently reviewable and does not authorize friend request or friendship schema. |
| P-021 | Blocking is a silent, independently directional action, while any active block in either direction creates symmetric separation. Users privately manage only their outgoing blocks; block and unblock are idempotent, unblock removes only the caller's row, and no relationship is restored. | Exact discovery, relationship summaries, friend requests/contact, accepted shared-list separation, and public profile/template/feed visibility exclude either-direction blocks. Missing and block-suppressed targets produce no profile projection; only the blocker-private outgoing-block RPC may return the blocked profile. Reporting remains a separate action under P-053 rather than an implicit block. |
| P-022 | Initial discovery is exact canonical-username lookup only. It trims/lowercases input, excludes self, incomplete profiles, and either-direction blocks, and returns at most one profile ID, username, and display name. | No directory, prefix, substring, fuzzy, recommendation, unrestricted profile read, email, Auth metadata, timestamp, or onboarding-state disclosure is permitted. |
| P-023 | Friend requests and friendships use one persistent, versioned current row per unordered profile pair, separate from directional blocks. Physical states are `pending`, `friends`, `cancelled`, `declined`, and `ended`; no separate request/friendship tables or relationship event log are introduced. The most recent requester remains available internally after transition. | Duplicate sends and repeated completed caller-authorized actions are idempotent, crossed requests become friendship, senders cancel, recipients accept/decline, either friend may end, and stale conflicting actions cannot overwrite newer state. A reopened pending send accepts the immediately prior dormant version for its retry or opposite crossed send, while older values remain stale. Every real transition increments version and updates a server-owned state-change time; no-op retries change neither. |
| P-024 | Reopening is controlled by the prior outcome: either participant may resend after cancellation; only the decliner may resend after decline; only the friendship ender may resend after ending. Blocking pending changes it to cancelled, blocking friends changes it to ended with the blocker controlling reopening, dormant states do not transition, and unblocking restores nothing. Client projections use caller-relative `can-send`, `incoming-pending`, `outgoing-pending`, `friends`, or `unavailable` status without revealing raw declined/ended state or the reopening controller to the other participant. | `unavailable` protects dormant decline/end reopening details, while an active either-direction block suppresses the relationship/profile summary entirely. Active blocks are rechecked inside every protected transition. Only the current row, version, creation/state-change times, requester, and reopening controller are retained. Persistent notifications, hard-deletion cleanup, shared lists, the shell, and Realtime reconciliation are delivered by later decisions; push and public profiles remain separate. |
| P-025 | Every real friend-request transition into `pending` creates one persistent in-app notification for its recipient and relationship version. Duplicate sends and crossed sends create no additional notification; an eligible dormant reopening creates one for the new version, and existing relationship history is not backfilled. The versioned relationship row remains the only action authority. | Notification Accept/Decline reuses the exact version-checked friendship boundary, so duplicate taps, retries, and stale rows are safe. Notification records contain references rather than copied profile, email, Auth, or message content. |
| P-026 | Friend-request notifications are newest-first with bounded deterministic keyset pagination. Opening a successfully loaded page marks only its displayed IDs read; reads are caller-owned and idempotent. The badge counts the caller's unread, visible, unexpired notifications. Active either-direction blocks permanently suppress existing pair notifications, and notifications expire exactly 180 days after creation. | Suppressed and expired rows are absent from listing and badge counts; unblocking restores nothing. User-facing archive, delete, mark-unread, preferences, physical cleanup, other notification types, push delivery, and account-lifecycle retention remain outside this slice. |
| P-027 | Account export and deletion are separate slices. Any authenticated email-verified user, including one with incomplete onboarding, can download a UTF-8 JSON document with product ID, positive schema version (initially `1`), server UTC export time, allowlisted own Auth/profile fields, outgoing blocks, active caller-visible relationships, and visible recipient notifications. Arrays are deterministic and never null. | Export preserves current block, relationship, notification, and logical-expiry privacy; it excludes credentials, tokens, sessions, Auth metadata, other users' Auth data, incoming blocks, dormant relationship internals, hidden notifications, logs, and future aggregates. It is a product download, not a guarantee of legal/regulatory compliance. |
| P-028 | Permanent account deletion follows only after export is merged and manually verified. It is immediate hard deletion after exact canonical-username confirmation for completed profiles or exact email confirmation for incomplete profiles, current-password reauthentication, and a session no older than ten minutes. It cascades current aggregates, reserves a completed username for 30 days without email/user ID, restores nothing on re-registration, and invalidates other sessions on later validation. | The prerequisite has been satisfied. The authenticated server-only `delete-account` boundary and one additive schema migration cover every current aggregate. Hosted QA uses separately authorized disposable accounts and never Fernando or Susana. |
| P-030a | Self-service deletion is immediate, permanent, and available to every signed-in email-verified user, including incomplete onboarding. Completed users confirm with the exact stored canonical username; incomplete users confirm with the exact stored Auth email. The current password is forwarded unchanged only to Supabase Auth for reauthentication, export is offered but optional, and a final irreversible acknowledgement is required. | No trimming/case folding, grace period, soft delete, restoration, password forwarding to application services, or target-user selection is permitted. Success clears local session-bound state. Lost responses and resume checks use authoritative Auth validation; offline failures preserve a valid local session. |
| P-031a | Deleting a completed profile reserves only its canonical username for exactly 30 days; incomplete profiles create no reservation. Active reservations reject onboarding, expired reservations permit reuse, and a migration-managed daily 03:17 UTC database Cron job physically removes expired rows. | The private record contains only canonical username and expiry, with no email, Auth/profile ID, display name, or copied former-user data. Concurrent deletion/claim and repeated reservation attempts must preserve the hold. |
| P-033a | The first active-list slice is owner-only: one fully onboarded profile owns each list, with no membership, role, invitation, assignment, note, mention, Realtime, offline-cache, template, or Payment Control schema. Owners can create, read, rename, archive, restore, and permanently delete lists. Titles are trimmed to 1-80 characters, duplicates are allowed, status is exactly `active` or `archived`, archived lists remain readable but reject every mutation, and deletion removes the current item aggregate. | This deliberately narrow phase does not settle future collaborative roles, shared-resource blocking, invitations, ownership transfer, or member lifecycle. Auth-root profile deletion cascades owned lists and items. |
| P-034a | Owner-only list items support add, edit, deterministic drag reorder, complete, reopen, and permanent delete. Names are trimmed to 1-120 characters and duplicates are allowed. Quantity is a positive integer number of thousandths, defaults to `1000`, and is limited to `1` through `999999999`; Flutter accepts at most three decimal places without binary floating point and suppresses insignificant display zeros. Unit is null or one stable code: `piece`, `kg`, `g`, `l`, `ml`, `pack`, `box`, `bottle`, `can`, or `bag`. | Completion records database-owned time and the authenticated owner; reopening clears attribution. A future missing actor may leave completion time with a null actor. Initial order follows successful creation order; reorder is atomic and requires the exact current item-ID set. P-043 adds the finite current-item capacity without changing existing-row behavior. No detailed item event history exists yet. |
| P-035 | Shared lists have exactly one owner and retained versioned access rows. Only the owner manages invitations/members and list metadata; accepted members can read and mutate active-list items. Owners cannot leave, members may leave even when archived, and ownership transfer follows P-041. Capacity is 20 including the owner and `pending`/`member` rows. | Accepted members see the owner and accepted participants only through profile ID, username, and display name. Pending recipients are visible only to the owner and themselves. Archived lists remain readable, reject content mutation and invite/accept, allow owner removal/member leave, and cancel pending invitations when archived. |
| P-036 | Only accepted friends may be invited and friendship is rechecked for every new/reopened invitation. Invitations persist without automatic expiry in one versioned current row with exact states `pending`, `member`, `declined`, `cancelled`, `removed`, and `left`. Recipients accept/decline, owners cancel, and dormant rows may be reinvited when friendship, block, capacity, and list-state rules permit. | Real transitions are version checked; duplicate sends and completed retries are idempotent. Pending invitations reserve capacity. Ending friendship cancels pending invitations but preserves membership. |
| P-037 | Blocking applies symmetric list separation without third-party removal authority: an owner blocking a member cancels/removes that person; a member blocking the owner cancels/leaves; a member blocking another member leaves every third-party list shared by both. Pair invitations are cancelled, unblock restores nothing, and notification text does not expose block direction or cause. | Block, relationship, participant, and notification effects are atomic and lock affected lists deterministically. Generic owner access-change/member-left wording is required; the blocker never removes another member from somebody else's list. |
| P-038 | A real transition to pending creates exactly one actionable list-invitation notification for that access version. Pending remains discoverable beyond normal expiry; resolution restores normal retention. Owners receive generic accepted/declined/member-left information, removed recipients receive generic information, and routine item changes create none. | The access row, never the notification, is action authority. Stale actions reconcile truthfully, unread state stays recipient-owned/idempotent, and block-driven cancellation is suppressed without disclosure. No push delivery or preferences are added. |
| P-039 | Account export schema version `3` preserves full owned-list export and adds privacy-minimal caller-relative access metadata for lists owned by others, excluding their items, owner/participant identities, and authority internals. Before permanent deletion, an authoritative warning reports the number of owned shared lists and total non-owner accepted participants losing access. | Owner deletion still proceeds and cascades owned lists; member deletion removes only their access rows. The warning discloses no identities. Export versions 1 and 2 remain strictly readable. |
| P-040 | Connected devices reconcile collaborative list, membership, friendship/block, and persistent-notification projections automatically through best-effort private invalidations. The experience does not require reopening a screen or manually refreshing while connected, but manual refresh and app-resume reconciliation remain fallbacks. | Delivery is not guaranteed and the event carries no domain content. RPC reads and existing versioned writes remain authoritative; transient Realtime failure preserves cached UI without a global spinner or repeated error message. |
| P-041 | Only the current owner may transfer an active list to an exact current accepted member after an explicit named confirmation. Friendship is not re-required, but onboarding, either-direction block, accepted access, list state, capacity, and exact-version rules are rechecked atomically. The recipient becomes the sole owner, the former owner remains an accepted member, and capacity/content/other access remain unchanged. | The new owner immediately gains and the former owner loses every owner-only capability. No password reauthentication or offline transfer exists. A real transfer creates one informational new-owner notification and private invalidations; failed, stale, rejected, duplicate, or rolled-back attempts create neither. A transfer back is a new confirmed action by the then-current owner. |
| P-042 | Private templates are personal reusable ordered item collections with duplicate non-empty names, zero or one private category, blank/empty validity, owner-only management, and independent snapshot semantics. Accounts may own 100 templates and 25 categories; category names are per-owner unique after case/extra-space normalization, empty categories remain visible, and deleting a category atomically moves templates to Uncategorized. | Template items contain only name, exact quantity, and order. Categories/templates create no notification. Public, shared, sent, friend-owned, collaborative, group, rating, feed, and discovery behavior remains deferred. |
| P-043 | A shopping list and a template each admit at most 200 current items. Completed list items still count; physically deleted rows free capacity immediately. At capacity existing rows remain editable/completable/reorderable/deletable. Legacy over-capacity lists remain intact and accessible but reject additions until below 200. Save-as-template and create-list-from-template require 1-200 selected items; existing-list import requires at least one selection no larger than authoritative remaining capacity. | All ordinary creates and copy/import paths enforce capacity transactionally. Duplicate-name rows each consume one place. Exact-capacity operations succeed; stale or concurrent overflow fails atomically with no partial rows, version changes, selection reduction, or Realtime message. No migration deletes, truncates, or rewrites existing data to apply the limit. |
| P-044 | **Split** is the current product/UI name for the optional Payment Control expense ledger inside one main list. The owner enables it; its single currency is `CHF` or `EUR` with two minor-unit digits and may change only while no expense or settlement history exists. Expense descriptions are trimmed to 1-120 characters, totals are 1-999999999 integer minor units, and at most 200 current expenses exist per list; physical deletion frees capacity without destructive legacy cleanup. | Current unblocked owner/members may mutate expenses only while active; archived lists remain readable. Split creates no persistent notifications and still defers conversion, guests, receipts, categories, charts, and recurring expenses. |
| P-045 | A new Split expense has one current eligible payer and a non-empty subset of current eligible beneficiaries; the payer may be excluded. Equal split assigns `floor(total/count)` then one remaining minor unit to each earliest immutable list-participant UUID. Each expense contributes paid minus owed to net balance; positive is receivable, negative is owed, zero is settled, and all nets sum to zero. | Removed members remain understandable in existing history but cannot be newly attached; edits may retain only historical roles already on that expense. Reaccepting the same live account reuses its financial identity. Permanent account deletion anonymizes that identity while retaining integer arithmetic. |
| P-046 | A Split settlement records external bookkeeping, never a processed or proven payment. Any current unblocked owner/member may record a full or partial payment from any same-list current/historical debtor to any current/historical creditor; the server derives and displays the recorder. Amount is a positive integer no greater than the smaller authoritative outstanding side, with no separate expense-size cap; an optional trimmed note is at most 120 characters. | Settlements are immutable. The original recorder or current owner may append one full reversal with a required trimmed 1-120-character reason; no edit/delete exists. Archived lists are read-only, list deletion cascades history, account deletion anonymizes participant/recorder identities, there is no lifetime count cap, and bounded keyset pages read history. The first settlement permanently locks currency even after reversal. No payment provider, approval/dispute flow, backdating, notification, or offline mutation queue is introduced. |
| P-047 | Split supports Equal and exact Custom amounts. Equal remains the default and uses P-045's canonical server algorithm. Custom accepts only positive CHF/EUR integer minor-unit shares whose unique selected participants sum exactly to the expense total; zero deselects a participant, and no percentage, weight, ratio, proportional allocation, rounding, automatic correction, or partial save exists. The payer may remain outside the beneficiary set. | Explicit share rows are the durable truth; allocation mode is editor input state and is not persisted or added to export. Canonical-equal detection includes UUID-ordered remainder distribution, so identical custom input may reopen as Equal. Equal-to-Custom prefills current canonical shares; Custom-to-Equal is recalculated server-side on confirmed Save. An already-attached historical beneficiary may remain and be adjusted, but no new ineligible participant may be attached. Legacy clients retain equal create/edit behavior but cannot overwrite a non-equal expense. |
| P-048 | Active-list items have zero to 20 current assignees, bounded by the current eligible participant set. Every current unblocked owner or accepted member may set the complete eligible assignment set on any active completed or uncompleted item, including assigning/unassigning themselves or another current participant. Assignment is current state only: access loss, explicit unassignment, item/list deletion, or account deletion removes it; ownership transfer preserves it; templates never copy it. | A real newly added assignment creates one 180-day informational notification only for a different-user assignee. Self-assignment, unassignment, retry/no-op, cleanup, and rejection create none. Names are resolved live; block or recipient access loss permanently suppresses the row. The UI is localized in English/Portuguese, accessible with large text/light/dark themes, and adds no history, note/mention, deep link, push, preference, reminder, template-assignment, or offline-mutation behavior. |
| P-049 | Every active list has one optional shared General Note. The current unblocked owner and accepted members may edit it only while active; archived lists display it read-only, and pending/removed/historical/blocked/foreign users have no access. CRLF/CR normalizes to LF, outer whitespace is trimmed, empty becomes null, internal whitespace/Unicode is preserved, and the limit is 2,000 Unicode code points. A real text/resolved-link change advances note and parent-list versions once; no-op/equivalent retry changes nothing, and payload-different stale work returns `40001`. | The UI is multiline, localized in English/Portuguese, accessible in light/dark/large-text/screen-reader use, and has no per-item note, rich text, comments, history, attachment, reaction, receipt, deep link, push/email delivery, or offline mutation queue. Dirty drafts survive remote note/version/mention-eligibility conflicts with one deterministic recovery choice; caller access loss/list deletion and remote archive use the established one-time safe exit/read-only behavior. |
| P-050 | Resolved General Note mentions use explicit stable profile IDs without username snapshots. Submitted IDs are untrusted, deduplicated independent of order, current completed unblocked owner/member identities, and must appear as complete current canonical `@username` tokens in saved text. Matching is ASCII case-insensitive; boundaries are start/end or neither an ASCII letter, digit, underscore, nor `@`, rejecting email-like, doubled-`@`, and partial tokens. Manually typed text may remain unresolved; repeated tokens create one link; self-mentions resolve but do not notify. Completed usernames remain immutable, while live display-name changes update rendering. Access loss removes only the structured link, preserves literal text, and never auto-reactivates after reinvite, unblock, or old-name reuse. | Only a real newly resolved other-user link creates one `list_note_mentioned` row at the resulting note version. Retained/removal/self/repeated/retry/no-op/rejected work creates none. Actor/recipient access loss or either-direction block permanently stores suppression; no reinvite/unblock clears it. Templates remain item-only: saving never copies notes/mentions, creating starts null/version-1/no-links, and import preserves the destination note. |
| P-051 | Templates remain private by default and only their owner may publish, unpublish, edit, or delete them. Public templates appear only on the owner's block-aware public profile. Every fully onboarded authenticated friend/nonfriend has identical access when neither direction is blocked; blocking suppresses profile, listing, detail, copy, and direct-ID access. Missing/private/deleted/incomplete/blocked resources are indistinguishable. Publication uses nullable `published_at`; real publish/unpublish versions once, first/republication receives a fresh server time, ordinary public edits preserve it, and achieved-state retry is a no-op. | Publication requires a named template with 1-120 Unicode code points, including valid zero-item templates. Existing private legacy names are unchanged, while public edits remain eligible and owners can always unpublish/delete. Public RPCs expose only profile ID/current username/display name, template ID/name/version/item count/publication time, and ordered item name/exact quantity/position. Global search, feed, ranking, rating, review, comment, recommendation, anonymous, and web access are excluded. |
| P-052 | Save a copy creates one atomic caller-owned private Uncategorized deep copy of the complete zero-to-200-item public source, including source-owner self-copy. Every template/item receives a new UUID; name, quantities, and order are preserved, while category, notes, mentions, assignments, membership, Split data, notifications, and every source/provenance identity are excluded. | Mutation-time onboarding, both-direction block, publication, exact version, source capacity, and destination quota checks are authoritative. A caller/request UUID is fingerprint-bound for lost-response idempotency; conflicting reuse fails. Completed copies never follow source/relationship/account changes. Publication/view/copy create no notification or public/global Realtime topic; owner/copier invalidation reuses the private account transport. Export v9 adds only caller-owned `is_public`/`published_at`; reporting/takedown follows P-053/P-054 without changing independent copies. |
| P-053 | A completed authenticated non-owner may report only an exact currently accessible Public Template revision for one of eight stable reasons. Explanation is trimmed plain text up to 500 characters, optional for the first six reasons and required for copyright/trademark or other. The server retains each individual report, snapshots exactly the visible name and ordered item names/quantities, fingerprints it, groups matching immutable template/revision/content, and permits one report per reporter/template revision. | A successful report hides that source only from that reporter across profile/detail/copy/refresh/resume; it neither blocks the owner nor mutates independent copies, relationships, or publication. Block remains a separate confirmed option. Copyright reporting is an in-app signal rather than a formal legal notice. V1 has no withdrawal, unhide, attachment/evidence upload, appeal, automated takedown, strike/suspension, similarity matching, or cross-template enforcement. |
| P-054 | Moderator authority is an initially empty private allowlist of immutable Auth UUIDs. Moderators review bounded Open/Taken down/Closed groups with immutable snapshot/current comparison and individual reporter detail. Dismiss closes one group; takedown restricts one template, unpublishes it, closes all its open groups, and notifies the owner once; restore removes the restriction and notifies once without republishing. Every action is confirmed and requires a private 1-1,000-character note; takedown also requires one stable owner-facing reason. | Owners may privately read/edit/delete restricted templates but cannot publish. Reports and dismissals notify nobody. System-authored outcome notifications expose only safe template name/general reason. Open evidence and active restrictions are retained; fully closed inactive evidence is retained for 24 months, then a privileged idempotent operation removes detailed personal/content evidence and may retain only nonidentifying aggregate tombstones. The operation is implemented and A-062 separately owns scheduling; moderator assignment, hosted rollout, and physical QA remain controlled steps. Export v10 adds only the caller's own submitted reason, nullable explanation, and date. |
| P-055 | A completed user may send one owned private or public zero-to-200-item template snapshot to one current mutually unblocked friend. Offers are `pending`, `accepted`, `declined`, `revoked`, or `unavailable`; pending never expires automatically. The recipient alone Accepts/Declines, the sender alone Revokes, and one pending sender/source/recipient triple exists at a time. Accept creates one independent private Uncategorized recipient copy, including a blank copy, while recipient-capacity failure leaves the offer pending. | Active moderation rejects new sends and closes/hides affected pending offers. Source deletion closes pending offers; accepted copies survive. Friendship loss or either-direction block closes pending offers and permanently hides all pair history; refriending/unblocking never restores it. Accept/Decline do not notify the sender. Persistent Received and minimal Sent status exist, terminal history lasts 180 days, and the sender never sees `accepted_template_id`. |
| P-056 | Community has a separate friends-only feed of currently public, moderation-eligible templates owned by current accepted friends; it excludes the viewer, either-direction blocks, and templates successfully reported by the viewer. “Recent” means publication time, ordered by `(published_at DESC, template_id DESC)`. Ordinary edits preserve position and republication receives a new timestamp. | Pages default to 20 and accept 1-50 rows with an exclusive timestamp/UUID cursor, no total or age cutoff. The projection is authoritative at read time: friendship loss, block, unpublish, delete, restriction, or account deletion removes an entry on refresh, while unblock/refriend may restore current eligibility. V1 has no stored/ranked/recommended feed, retention, notification, push, publication fan-out, or new Realtime topic. |

### Architecture and delivery

| ID | Decision | Consequence |
| --- | --- | --- |
| A-001 | Use Flutter stable for Android and iOS only. | Other platform folders and support are outside current scope. |
| A-002 | Android Production application ID/namespace and the current iOS bundle identifier are `com.ferbatech.listandsplit`; the separate Android Dev application ID is `com.ferbatech.listandsplit.dev`. | No `com.example` or identifier containing `list_and_split` may remain. The Dev suffix creates intentionally isolated Android storage and does not migrate the legacy unsuffixed Dev installation. |
| A-003 | Use Material 3 with architectural support for light and dark themes. | The root app owns both theme modes. |
| A-004 | Organize code feature-first with MVVM-style presentation. | Views stay declarative; feature state/intent belongs in view models/providers; avoid speculative layers. |
| A-005 | Use Riverpod for state management and dependency injection. | Dependencies and feature state are provided/tested through Riverpod rather than globals. |
| A-006 | Use `go_router` for navigation and future deep links. | Route state is centralized and should be testable. |
| A-007 | Repositories are the client data source of truth. SQLite caching is introduced later for offline-tolerant active-list use. | UI cannot depend directly on Supabase/SQLite; cache and sync stay behind repositories. |
| A-008 | Use Supabase Auth, PostgreSQL, RLS, Realtime, Storage, database functions, and Edge Functions as backend capabilities. | Concrete use is phased; listing a service does not require premature scaffolding. |
| A-009 | Git-committed database migrations are the schema source of truth. | Avoid Dashboard-only schema edits; schema review and reproducibility are mandatory. |
| A-010 | Every application table has RLS and least-privilege policies from its creating migration. | Authorization is enforced server-side and tested for allowed/denied identities. |
| A-011 | Client Supabase configuration is supplied through compile-time `dart-define` values; real values are not hardcoded. | Commit placeholders only, and never include a service-role/privileged key in Flutter. |
| A-012 | Use only current stable compatible package versions, not prerelease releases. | Dependency upgrades must preserve compatibility and pass verification. |
| A-013 | The app remains runnable without Supabase credentials and presents a clear development-configuration destination when required public values are unavailable. | Missing configuration is an explicit application state rather than a startup crash or a mocked authenticated experience. |
| A-014 | Backend work targets local development and only an explicitly authorized hosted development project; production Supabase and Firebase projects require separate authorization. | Environment creation and remote schema changes remain deliberate and reviewable. |
| A-015 | CI verifies dependency resolution, formatting, analysis, and unit/widget tests; relevant Supabase changes also run reproducible migration/database/RLS tests. | Local and CI verification use the same stable Flutter and database expectations. |
| A-016 | Work uses the task's authorized branch, conventional commits, and a draft pull request; no force-push or automatic merge. | Publishing remains reviewable and non-destructive. |
| A-017 | Public Supabase client configuration uses `APP_ENV`, `SUPABASE_URL`, and `SUPABASE_PUBLISHABLE_KEY`. Android Dev uses `com.ferbatech.listandsplit.dev://auth-callback`; Android Production and current iOS use `com.ferbatech.listandsplit://auth-callback`. | Only public/publishable client values enter Flutter. The established `auth-callback` host remains stable; full iOS flavor/scheme work is deferred. |
| A-018 | Session routing is centralized in `go_router` and gates destinations in this order: backend configuration, authentication, email verification, then profile onboarding. Password-recovery callbacks enter the new-password flow. | Redirect logic is deterministic, repository-backed, and testable without a live backend. |
| A-019 | The initial profile is a one-to-one application record for an `auth.users` identity. A server-owned creation mechanism allows nullable onboarding fields; RLS and explicit Data API grants permit authenticated owner read and approved-field update only. | Anonymous and cross-user access, plus client insert/delete, are denied. Authorization does not rely on user-editable Auth metadata. |
| A-020 | Active directional blocks use a narrowly scoped application table with RLS and RPC-only client access. Exact discovery and outgoing-block management use minimal privileged projections derived exclusively from `auth.uid()`, without broadening owner-only profile SELECT. | Privileged functions pin an empty `search_path`, fully qualify objects, validate completed profiles, revoke default execution, grant only exact authenticated signatures, and return only approved fields. |
| A-021 | `public.user_relationships` has normalized low/high profile participants as its composite key, check-constrained text state, participant-constrained requester/reopening controller, positive `bigint` version, and server-owned creation/state-change timestamps. Participant foreign keys remain non-cascading until the accepted future hard-deletion slice implements aggregate cleanup. | The creating migration enables RLS, revokes table privileges from `PUBLIC`, `anon`, `authenticated`, and `service_role`, adds a restrictive `FOR ALL` anon/authenticated policy with both predicates false, preserves owner-only profile policies, and adds only the reverse-participant index justified by high-participant relationship listings. |
| A-022 | Relationship data is accessible only through exact authenticated RPCs for one caller-relative summary, active incoming/outgoing/friend lists, send, cancel, accept, decline, and end. Relationship and block mutations share deterministic pair normalization and transaction-level locking and recheck blocks in the critical section. Non-send mutations require the exact expected version; send accepts nullable expected version, allowing null only for first/duplicate/crossed pending behavior, requiring exact version for dormant-state reopening, and accepting the immediately prior dormant version for a resulting pending retry/cross. | Functions derive identity only from `auth.uid()`, require a verified fully onboarded caller and an onboarded target, reject self-pairs, fully qualify objects, pin an empty `search_path` when privileged, revoke execution from `PUBLIC`, `anon`, and `service_role`, grant only exact authenticated signatures, and return minimal caller-relative projections. Privacy-safe dormant `unavailable` results omit version and state-change metadata; active blocks return no summary row or profile fields. Concurrent first/crossed sends and block transitions remain atomic; stale reopening cannot overwrite dormant state. |
| A-023 | `public.user_notifications` is an RPC-only, RLS-enabled table initially constrained to `friend_request`. It stores database-generated identity/times, non-cascading recipient and actor profile references, the normalized relationship pair and positive creating version, nullable read/suppression times, exact 180-day expiry, and a uniqueness boundary for one recipient/type/pair/version. | Direct table privileges are revoked from `PUBLIC`, `anon`, `authenticated`, and `service_role`; a restrictive `FOR ALL` policy explicitly rejects `anon` and `authenticated`. Only justified recipient listing/badge indexes are added, and notification rows never duplicate profile text, email, Auth metadata, or arbitrary messages. |
| A-024 | Exact authenticated RPCs list a minimal block-aware notification projection with bounded `(created_at, id)` keyset pagination, return the caller's unread visible count, and mark a bounded caller-owned ID array read. The existing `send_friend_request(uuid,bigint)` and `block_profile(uuid)` signatures are replaced only in an additive migration to create and suppress notifications under the established pair lock. | The RPCs derive the caller from `auth.uid()`, require a verified fully onboarded profile, use hardened definer-rights boundaries with empty `search_path`, explicit object qualification, revoked defaults, and exact authenticated grants. Send and block behavior, comments, ownership, concurrency, and public signatures remain otherwise unchanged. |
| A-025 | Account export is one parameterless `public.export_own_account_data() returns jsonb` RPC. It derives `auth.uid()`, requires one confirmed Auth user and exactly one profile without requiring onboarding, builds every object from an explicit allowlist, and synchronously returns a stable read-only document without server persistence. | The `postgres`-owned definer-rights function has empty `search_path`, qualified objects, revoked default execution, and an exact `authenticated` grant. It adds no table grants and preserves existing caller-relative/block-aware projections. |
| A-026 | Flutter validates schema version `1` behind an account repository, pretty-prints UTF-8 through an injectable temporary-file/share service, and presents one privacy-safe JSON file through the native Android/iOS share sheet. Export state is session-identity-scoped, prevents overlap, and never retains payload JSON globally. | Only compatible pinned path/share dependencies are added. Temporary cache is OS-managed rather than guaranteed securely erased; payloads never enter logs, analytics, errors, source control, or identifying filenames. |
| A-032a | Auth Admin hard deletion of `auth.users` is the atomic root. Cascading foreign keys remove current profiles, either-direction blocks, either-participant relationships, recipient/actor notifications, and notifications whose relationship is removed. Before profile deletion, a hardened trigger creates the private 30-day username reservation. A narrow authenticated RPC derives identity from `auth.uid()` and `auth.jwt()` `session_id`, validates the exact matching `auth.sessions` row's actual creation time is at most ten minutes old, and returns only success before the authenticated Edge Function invokes hard deletion for that same caller. | The RPC accepts only confirmation and never mutates. JWT `iat`, token refresh time, and user `last_sign_in_at` cannot establish freshness. The POST-only Edge Function disables the legacy platform JWT check and uses a pinned `@supabase/server` `auth: 'user'` wrapper as the sole authentication boundary under the publishable/secret-key system. The wrapper verifies the user session, provides the caller-scoped and server-only admin clients, never accepts a target ID, and maps failures to privacy-safe outcomes. |
| A-035a | `public.active_lists` and `public.active_list_items` form one RPC-only owner aggregate with UUID identities, positive `bigint` versions, server-owned timestamps, checked text/state/quantity/unit/order invariants, owner/list cascades, and only query-justified foreign-key and ordered-keyset indexes. Both tables have enabled and forced RLS, explicit restrictive `FOR ALL` rejection policies for `anon` and `authenticated`, and no direct privileges for `PUBLIC`, `anon`, `authenticated`, or `service_role`. Narrow authenticated PostgreSQL RPCs derive only `auth.uid()`, require a confirmed fully onboarded caller, verify ownership, return allowlisted projections, and reject archived mutations. | List metadata changes increment only list version. Item create/delete/reorder increment list version; item edit/complete/reopen increment both list and item versions. Real changes update their corresponding timestamps once; completed retries/no-ops update neither. Expected versions use SQLSTATE `40001` for stale conflict. Creation uses caller-generated request UUIDs only for payload-bound idempotency; conflicting reuse is rejected. List row locking precedes item row locking, item locks follow UUID order, and reorder accepts exactly the current unique item set and writes deterministic positive integer positions in one short transaction. |
| A-036a | The authenticated application uses `StatefulShellRoute.indexedStack` with four state-preserving branches: Lists, Templates, Community, and Profile. Notifications remain a root overlay opened from the existing bell; configuration, Auth, verification, recovery, onboarding, and account-error gates remain outside the shell. Lists and Templates use feature-first repository/Riverpod boundaries scoped to the verified session and are invalidated on sign-out/deletion/account change. Account export schema version `2` adds one deterministic `active_lists` array containing both statuses and ordered allowlisted items, while Flutter deliberately retains strict version-1 fixture parsing. | Android back and independent tab stacks are controlled by the centralized router. Community and Profile retain existing functionality. The export includes exact integer `quantity_thousandths` and excludes creation request IDs and authorization internals. The existing Auth-root deletion cascade extends through the new owner foreign key without changing the deletion migration or Edge Function. |
| A-037 | `public.active_list_participants` retains one current versioned access lineage per list/profile, including the internal `owner` state accepted by A-044, with check-constrained state, positive monotonic version, and database-owned timestamps. It is RPC-only with enabled/forced RLS, an explicit restrictive direct-access rejection policy, revoked table privileges, owner/list cascades, and query-justified indexes. | Exact `postgres`-owned authenticated RPCs derive `auth.uid()`, require a verified onboarded caller, pin empty `search_path`, fully qualify objects, return minimal projections, and use pair-then-list deterministic locking. List locking serializes capacity and transitions; no role, event-log, presence, assignment, or offline tables are introduced. |
| A-038 | Existing list read/item RPCs authorize owner or current accepted member and recheck blocks; list metadata and access-management RPCs remain owner-only. The persistent notification table is extended with type-specific nullable relationship/list references and exact access versions; pending invitation visibility overrides normal expiry only while that exact access remains pending. | Direct table CRUD remains denied and only exact authenticated function signatures are granted. Notification actions call the access repository boundary and refresh notification, badge, and list state without permanent busy state. |
| A-039 | Export schema version `3` adds an allowlisted `shared_list_access` root, and a parameterless authenticated impact RPC returns only two non-negative counts for deletion confirmation. Auth-root cascade deletes owner aggregates and member rows with completion attribution `SET NULL`; the Edge Function and reauthentication boundary do not change. | Flutter strictly parses versions 1/2/3, fetches impact before final confirmation, and clears every list/member/invitation provider on session/account change. Hosted export and deletion are never invoked by delivery verification. |
| A-040 | List synchronization uses private Supabase Broadcast channels carrying opaque invalidation events only. RPC repositories remain authoritative and app resume/manual refresh reconcile state; Broadcast never carries list/item/member/profile/notification content or grants authority, and there is no offline mutation queue. | The concrete receive authorization, database fanout, session lifecycle, and coalescing contracts are fixed by A-041 through A-043. |
| A-041 | Each completed authenticated session uses one private channel `account:<auth.uid()>`, event `invalidate`, and application payload exactly `{"v":1}`. The sole `realtime.messages` policy is authenticated `SELECT` for extension `broadcast` when `realtime.topic()` equals that caller-derived topic. | Anonymous/other-account joins, authenticated sends, Presence, public channels, per-list authorization, Postgres Changes, Replay, and application payload data are denied or absent. Cached channel authorization depends only on the active identity; the project public-access setting must be disabled. |
| A-042 | Postgres-owned hardened trigger functions call `realtime.send(..., true)` in the authoritative transaction and derive affected account recipients for list, participant, notification, relationship, block, profile, and deletion-cascade changes. Item fanout follows the mandatory parent-list version update. | Rollback/rejection commits no message, completed no-ops need none, and duplicates are acceptable. Helpers have empty `search_path`, qualified objects, comments, revoked Data API execution, no dynamic SQL, and add no table, publication, outbox, queue, history, or payload authority. |
| A-043 | A narrow injected Supabase adapter is the only Flutter channel caller. Supabase initialization injects a testable WebSocket transport with a named conservative handshake deadline, so a stalled ready future fails within a bound and the pinned SDK can release and retry it. A session-scoped coordinator starts after verified completed onboarding, removes the old channel before account replacement, reconciles authoritatively after every valid event, successful subscription, and app resume, and disposes on every session reset. Mounted feature projections register repository refresh tasks under one generation-safe single-flight pass with one dirty follow-up and bounded cooldown. | The pinned SDK remains authoritative for authenticated-session token propagation. Channel errors, timeouts, closure, and resume are recoverable through serialized idempotent replacement that preserves exactly one account channel and callback registration. Privacy-safe diagnostics expose lifecycle context but no token, key, topic identifier, profile ID, payload, or private content. Malformed payloads and stale generations are ignored, cached UI/manual refresh remain usable, local mutations are not overwritten while in progress, and revoked access or a remote archive exits open list detail once. Background notification reconciliation writes only unread rows so it cannot self-trigger an invalidation loop. |
| A-044 | `active_lists.owner_id` remains the sole ownership authority. The exact authenticated transfer RPC accepts list/target IDs plus expected list and target-access versions, uses the established pair-before-list deterministic lock order, and retains the promoted participant row in an internal `owner` state. The target and former-owner access lineages advance monotonically, the list version advances exactly once, deferred consistency permits no committed owner/member duplication, and the result allowlists only identities and authoritative versions. | No participant row or notification reference is deleted during transfer, preventing ABA/version reuse. The existing capacity count is unchanged. Export schema version `3` excludes the internal current-owner row from shared access and classifies the former owner as shared; deletion impact and Auth-root deletion follow the new `owner_id`. Existing transaction-local list/participant/notification triggers fan out the unchanged private `{"v":1}` invalidation contract. |
| A-045 | `template_categories`, `templates`, and `template_items` form one private RPC-only aggregate with UUID identities, payload-bound creation request IDs, positive monotonic versions, database timestamps, owner/template cascades, same-owner category placement, normalized per-owner category uniqueness, and only query-justified indexes. All three tables enable and force RLS, explicitly reject direct `anon`/`authenticated` access, revoke every API-role table privilege, and expose only exact `postgres`-owned authenticated functions with empty `search_path` and allowlisted results. | Caller-scoped and row locks serialize 25-category, 100-template, and 200-item limits. Category deletion atomically uncategorizes and versions affected templates. Template metadata/item mutations advance the appropriate template/item version once. Auth-root deletion removes only the caller's private aggregate. |
| A-046 | Exact PostgreSQL RPCs save accessible list snapshots as private templates, create new lists from private templates, and import private-template rows into active accessible lists. They validate unique 1-200 source IDs, exact source/destination versions, caller-derived access, state, quota/capacity, and payload-bound retry IDs under deterministic locks. The existing list-item creation RPC is replaced additively to enforce the same 200-row list capacity. | Copies contain only name, exact quantity, and order and create no source foreign key. Imports advance the destination list version exactly once and use existing list/account Broadcast fanout; private template/category mutations invalidate only `account:<auth.uid()>` and create no notifications. Export schema version `4` adds allowlisted private categories/templates/items, while Flutter keeps strict versions 1-3 compatibility. |
| A-047 | Split uses RPC-only settings, independently generated persistent list-scoped financial participants, expenses, and explicit share rows. Same-list composite keys protect every financial reference; live participant profile links are nullable, membership removal preserves snapshots, and account deletion clears links/snapshots without retaining a deletion timestamp. A-055 later coordinates that cleanup parent-first rather than through the original independent profile trigger. No mutable balance row exists. | All tables force RLS, explicitly reject direct clients, revoke API table privileges, and cascade from the list. Hardened exact authenticated RPCs lock list then settings then expense, recheck current access/state/eligibility/versions, allocate equal shares transactionally, and return allowlisted server-derived balances. Participant acceptance/transfer/reacceptance reuses one identity; payload-bound expense request UUIDs make lost-response retries idempotent. Rejected work changes no row, version, notification, or Broadcast message. |
| A-048 | Split is a feature-first repository/Riverpod flow nested under the immutable list-ID route and registered with the existing account reconciliation registry. Settings/expense writes fan out only opaque private account invalidations to current list accounts; authoritative reads own all content and permission decisions. Export schema version `5` nests allowlisted Split data only under fully exported caller-owned lists and keeps shared-list export metadata-only. | Decimal parsing never uses `double`; controllers guard overlap, reconcile stale/access/archive/delete outcomes, retain safe cached state on transport failure, and reset at session changes. Export versions 1-4 remain strictly readable. Account hard deletion either cascades owned Split rows or anonymizes another owner's retained participant before the profile disappears. |
| A-049 | Dedicated immutable settlement and append-only one-time-reversal tables extend the RPC-only Split aggregate with same-list composite references, forced RLS, explicit direct-access rejection, revoked API table privileges, and only exact hardened authenticated functions. Mutations lock list/settings/target deterministically, recheck access/state/current balances and expected Split version, derive every actor from `auth.uid()`, and bind list-scoped request UUIDs to payloads. | Identical lost-response retries are idempotent; rejected, stale, concurrent-conflict, or unauthorized work creates no row, version, notification, or Broadcast output. Balances include non-reversed settlement effects. Server suggestions greedily match largest debtors/creditors with participant-UUID tie-breaks: output is deterministic and integer-exact but not guaranteed mathematically minimum. History is bounded keyset-paginated. Existing private invalidations reconcile mounted clients. Export schema version `6` adds only allowlisted owned-list settlement/reversal history, excludes request IDs/derived output, and keeps strict versions 1-5 compatibility. |
| A-050 | One additive migration adds versioned expense create/update RPCs for server-derived Equal or exact normalized participant/minor-unit pairs without adding or altering tables, columns, allocation-mode fields, RLS policies, or Data API table grants. Hardened functions derive `auth.uid()`, use empty `search_path` and qualified objects, lock/recheck the existing list/settings/expense aggregate, validate cardinality, uniqueness, eligibility, historical roles, positive custom amounts, `bigint` bounds, conservation, archive/access and expected versions, revoke default/anonymous execution, and grant only exact authenticated signatures. | Creation request UUIDs bind the complete normalized allocation; update no-op comparison includes those full pairs. Rejected/stale/conflicting work changes no row/version and emits no notification or Broadcast. Legacy equal creation remains valid, while legacy update rejects a stored non-equal expense. Successful writes reuse opaque private invalidation and authoritative reconciliation. Existing read shapes and explicit share rows remain unchanged; export stays schema version `6` with strict versions 1-6 compatibility. The migration must deploy before distributing the new client. |
| A-051 | `public.active_list_item_assignments` is an RPC-only current-state junction keyed by `(list_id,item_id,assignee_profile_id)`, with same-list item and profile cascades, database-owned assignment time, reverse cleanup index, forced RLS, explicit direct-client rejection, and revoked API table privileges. Exact hardened v2 item list/create/update RPCs derive the actor from `auth.uid()`, accept the complete assignee UUID set, and validate caller access, active state, eligibility, uniqueness, item fields, and expected versions. Referenced profiles are deterministically key-share locked first; aggregate locking then follows list, item, and participant-row UUID order. | A real assignment-set update advances list/item versions once and returns deterministic allowlisted assignees; creation initializes its item once. Exact retries/no-ops change nothing and stale/invalid requests are atomic. Legacy list/create/update RPCs remain unchanged: legacy create assigns nobody and legacy update preserves the set. There is no assigned-by field, assignment history, soft delete, event log, template copy, new dependency, or platform configuration. |
| A-052 | The assignment-foundation participant access-loss cleanup removes current assignments on surviving items, permanently suppresses that recipient's assignment notifications, advances every affected item once and the list once, and reuses existing transaction-local private account invalidations. Mutation-only profile-FK paths prelock referenced identities before list locks and recheck changing participant snapshots. A-055 later folds assignment/mention/account-deletion work into one global hierarchy and replaces the earlier profile-deletion cleanup trigger. `list_item_assigned` rows are server-created only for real newly added other-user recipients and are unique by recipient/item/resulting item version. | The existing `account:<auth.uid()>` / `invalidate` / `{"v":1}` wire contract remains unchanged. Legacy notification listing/count excludes the new type; assignment-aware v2 listing/count includes live authorized actor/list/item projections and reuses caller-owned mark-read. Duplicate invalidations coalesce; no unassignment event, content payload, deep link, push, preference, or new channel exists. |
| A-053 | The unchanged `export_own_account_data()` remains schema version `6` for strict legacy clients. The separate parameterless `export_own_account_data_v7()` reuses the v6 allowlisted base and adds one deterministic non-null assignee array to each fully exported caller-owned item, containing only profile ID, username, display name, owner flag, and assignment time. | `shared_list_access` remains byte-for-byte metadata-only with no assignment, item ID/name, or assignment time. Flutter accepts versions 1-7, selects the v7 endpoint for the assignment-aware download, and preserves every prior version contract without exposing request IDs, direct table access, shared-list content, or a retained export. |
| A-054 | General Note scalar state belongs on `active_lists` as nullable normalized text, positive note version defaulting to `1`, and nullable server update time. `active_list_note_mentions` is an RPC-only current-state junction keyed by `(list_id,mentioned_profile_id)`, with reverse cleanup index, cascade roots, forced RLS, explicit direct-client rejection, revoked API table grants, and no username/token snapshot. Dedicated hardened authenticated read/update RPCs derive `auth.uid()`, validate current access and the complete canonical token/ID set, use empty `search_path`/qualified objects/exact grants, and preserve all legacy list signatures. | Text and links replace atomically. Invalid/stale work creates no row/version/notification/Broadcast; equivalent completed retry/no-op is unchanged. Rendering joins live username/display name. Templates and legacy APIs cannot see, copy, or clear invisible note state. |
| A-055 | Cross-identity writes follow one global lock hierarchy: non-locking preflight; relevant profiles in UUID order; canonical pair advisory lock only for relationship lifecycle; parent lists in UUID order; then items, participant/access rows, assignments, mentions, and Split children in deterministic internal order; finally notifications/suppression and Broadcast work. No path acquires a new profile, pair, or parent after child mutation starts. Note writes use profile→list→access→mention order and no pair lock. | One combined child-cleanup helper removes assignments and mentions and returns their effects. Each remove, leave, block, or account-deletion coordinator then completes ordered Split work, permanent suppression and notifications, advances each affected item once and the note only if its link changed, and advances the parent list at most once for the high-level transition. One parent-first profile-deletion coordinator supersedes the separate child-first Split-anonymization and later list-locking assignment triggers, covering surviving lists referenced by access, assignment, mention, Split history, or completion attribution and repairing the expense/account-deletion inversion. |
| A-056 | Notification v1 continues to exclude assignments and mentions; v2 includes assignments while explicitly excluding mentions; v3 includes both with matching listing/count authorization. `list_note_mentioned` stores references and the resulting note version, no content, is unique per list/recipient/type/version, and is visible only with current actor/recipient access, no block, live context, no suppression, and before its 180-day expiry. `mark_notifications_read` keeps its signature/limits/grants but applies those v3 note predicates. | Suppression stores `coalesce(existing, mutation_time)` and is never cleared. Suppressed/inaccessible/blocked/foreign/expired/deleted-context IDs cannot be marked read. Note/list/notification changes reuse only `account:<profile-id>` / `invalidate` / `{"v":1}` and authoritative coalesced reconciliation; no topic, channel, payload content, action, deep link, or push delivery is added. |
| A-057 | `export_own_account_data_v8()` preserves the corrected v7 allowlisted document and adds a nullable General Note object with text/version/update time plus minimal current resolved profile ID/username/display name only under caller-owned full lists. Existing v6/v7 RPCs and versions 1-7 remain unchanged; P-039 shared-list metadata remains byte-for-byte without note/mention data. Flutter requests v8, accepts versions 1-8, and keeps repository/controller/widget ownership of the accessible editor, suggestion, conflict, notification-v3, export, localization, and semantics behavior. | Removed links export only literal text. No shared-list note or mention identity, username snapshot, note excerpt notification, new dependency, route, Realtime transport, platform configuration, username editor, or extra locale is introduced. |
| A-058 | One additive migration extends `templates` with nullable publication time, a conditional public-name constraint, and a partial owner/publication/ID keyset index; adds the forced-RLS, explicit-rejection, no-API-grant `private.public_template_copy_requests` ledger; and adds exact v2 owner reads, desired-state publication, public-profile page/detail, atomic copy, and export-v9 RPCs. Every client RPC is `postgres` owned, hardened with empty `search_path`, qualified objects, caller-derived identity, completed-profile checks, revoked default/anon/service-role execution, and only exact authenticated grants. | Public pages are bounded 1-50 rows with stable timestamp/UUID cursors and no count. Cross-account copy locks profile UUIDs, canonical pair, destination quota, source template, then ordered items; it rechecks block/publication/version/quota and inserts destination/items/ledger atomically. Fingerprints are one-way and no raw source identity persists. Existing v1 template and export v1-v8 signatures/shapes remain unchanged; direct table access remains denied. |
| A-059 | Flutter adds strict public-profile/template/cursor DTOs and repository/controller boundaries, publication fields to v2 owned-template projections, immutable-ID Community routes, explicit owner publication UX, and copy request-ID reuse across uncertain transport. Session-keyed providers register with the existing account reconciliation registry; block/unavailable outcomes exit once and stale copy refresh requires review. | Community remains the selected shell destination and prior exact-search/friendship state is preserved when returning. Public detail is read-only except refresh/block/copy. Existing private account `invalidate`/`{"v":1}` remains the only transport: owner/copier/block changes reconcile affected accounts, while arbitrary viewer source changes rely on refresh/resume/action-time authorization. Flutter requests export v9 and strictly retains versions 1-8. English/Portuguese, light/dark, 200% text, semantics, and 48x48 targets are required without a dependency or platform change. |
| A-060 | One additive migration adds seven private forced-RLS, explicit-rejection, no-API-grant moderation tables for the empty Auth-UUID allowlist/audit, immutable revision-fingerprint report groups and reports, template restrictions, append-only decisions, and aggregate tombstones. Exact hardened client RPCs derive `auth.uid()`, use qualified objects/empty `search_path`, revoke default/anonymous execution, and grant only authenticated signatures; allowlist grant/revoke and retention maintenance remain `postgres`-only. Template-scoped locks, exact versions/fingerprints, and payload-bound action UUIDs serialize reporting, source changes, publication, dismiss/takedown/restore, retry, and concurrency. | Takedown closes every open group and enforces publication denial; restore never republishes. Source triggers record changed/unpublished/deleted state without rewriting evidence. Auth deletion nulls retained reporter/owner/moderator identities. The idempotent 24-month maintenance excludes open groups/active restrictions and leaves only nonidentifying counts, but this migration creates no cron schedule. Moderation tables never enter Realtime. Notification v4 adds strict actor-null owner outcomes while v1-v3 remain unchanged, and export v10 adds only caller-submitted reason/explanation/date while v1-v9 remain unchanged. |
| A-061 | Flutter adds strict report/moderation/export-v10/notification-v4 DTOs and repository boundaries; report intent remains under Public Templates while moderator queue/case state lives in a separate feature-first module. A server self-check gates the Settings entry, immutable group IDs drive protected routes, session-keyed controllers own bounded paging/actions, and every queue/action independently handles authorization loss. | Revocation clears cached moderation data and exits once. Report and action dialogs validate before one guarded submission, own notifier lifecycles through unmount, and prevent duplicate pop/message/navigation. Existing private `account:<profile-id>` / `invalidate` / `{"v":1}` triggers only authoritative refresh and carries no evidence. Old clients remain safe because server reads hide restricted/reporter-hidden content, old owner publication is denied, notifications v1-v3 exclude system outcomes, and exports v1-v9 are unchanged. English/Portuguese, light/dark, 200% text, semantics, and 48x48 targets remain mandatory without new dependency or platform configuration. |
| A-062 | One additive operational migration owns the stable `public-template-moderation-retention-daily` Cron job. It requires `postgres`, unschedules every same-name predecessor through `cron.unschedule`, and schedules `select * from private.maintain_public_template_moderation_retention();` once daily at `47 3 * * *`. | Repeated scheduling converges on exactly one active `postgres` job without invoking cleanup during deployment or changing evidence, function grants, eligibility, schema, Flutter, Realtime, or export behavior. Each environment, including Production, requires separate authorization and verification. Rollback is another forward-only migration that unschedules the stable name without deleting evidence or rewriting history. |
| A-063 | One additive migration adds forced-RLS, explicit-rejection, no-table-grant `template_sends` and immutable snapshot-item tables plus a private request ledger. Exact hardened authenticated Eligible-recipient, Send, Received, Sent, detail, Accept, Decline, Revoke, notification-v5, and export-v11 RPCs derive `auth.uid()`, use empty `search_path`, exact grants, versions, request UUID fingerprints, deterministic profile/pair/quota/source/send locks, and strict field allowlists. | Additive relationship/block/source/moderation triggers close or permanently suppress offers under P-055. Parent changes reuse opaque private account Broadcast. Notification v5 creates exactly one actor-null recipient row while v1-v4 remain unaware and sender outcomes create none. Export v11 is role-specific and provenance-free while v1-v10 remain unchanged. The private idempotent 180-day terminal cleanup is scheduled separately under A-065. PR #23 exposes only strict Dart domain/repository contracts; Flutter UI, routes, localization, notification actions, and export-v11 client integration belong to PR #24. |
| A-064 | Flutter exposes owned-template Send composition and a persistent Shared Templates area with Received/Sent pending and history projections, immutable-ID recipient detail, guarded Accept/Decline/Revoke actions, and bounded paging. Payload-stable request UUIDs survive uncertain transport; stale/capacity/unavailable outcomes refresh authoritatively and never fabricate success. | Session-keyed controllers register with the existing account reconciliation registry, reuse its single opaque private account channel, and avoid transport payload data or duplicate subscriptions. Notification v5 opens actionable recipient offers and terminal rows remain informational. Export requests v11 and strictly rejects provenance, internal IDs, or role-expanded shapes while preserving v1-v10 parsing. English/Portuguese, light/dark, large text, semantics, and confirmation for revoke/decline are required without a dependency or platform change. |
| A-065 | One additive operational migration owns the stable `template-send-retention-daily` Cron job. It requires `postgres`, unschedules every same-name predecessor through `cron.unschedule`, and schedules only `select * from private.maintain_template_send_retention();` once daily at `17 4 * * *`. | The 04:17 UTC slot does not collide with the existing 03:17 username-reservation or 03:47 moderation-retention jobs. Repeated scheduling converges on exactly one active `postgres` job without invoking cleanup during deployment or changing retention semantics, schema, grants, RLS, notifications, Realtime, exports, Flutter, or accepted copies. Each environment requires separate authorization and verification; Production remains unscheduled until explicitly deployed. Rollback is another forward-only migration that unschedules every stable-name job without invoking cleanup, restoring deleted rows, or rewriting history. |
| A-066 | One additive migration adds only the hardened stable `list_friend_public_template_feed(integer,timestamptz,uuid)` read-through RPC. It derives a verified completed caller from `auth.uid()`, reuses normalized `friends` relationships plus exact block/report/restriction/publication predicates, returns the strict P-056 allowlist, and remains `postgres`-owned with empty `search_path`, qualified objects, exact authenticated grant, and no direct-table change. | Flutter adds strict DTO/repository mapping, generation-safe paginated controller state, reconciliation registration, and the localized accessible `/community/friends-templates` Community route. Existing detail/copy/report RPCs remain action-time authorities. Existing private account invalidation covers applicable pair/profile changes; pull, toolbar, route load, and resume cover freshness without a feed table, index without plan evidence, export v12, notification, or new Realtime transport. |
| A-067 | Android has explicit `dev` and `prod` product flavors. Dev is `com.ferbatech.listandsplit.dev` / `List & Split Dev` and accepts only List & Split Dev project host `lzwsgxziqxpxwyalkfuy.supabase.co`; Production is `com.ferbatech.listandsplit` / `List & Split` and has no approved backend contract yet. A typed Dart contract cross-checks explicit `APP_ENV` against native Gradle flavor and final application ID through a narrow platform channel before Supabase initialization. | Missing, invalid, partial, malformed, mismatched, or environment-crossed configuration fails closed without revealing supplied values. Matching caller-controlled Dart values cannot bypass native Android identity. Production rejects the Dev project and remains unavailable until a later approved exact host is recorded. No hosted configuration, session migration, iOS flavor, signing, AAB, branding, dependency, SDK, database, or Edge Function change is part of this decision. |

The P-049/P-050 and A-054 through A-057 delivery is merged. The P-051/P-052 and
A-058/A-059 delivery is also merged. P-053/P-054 and A-060/A-061 are merged.
A-062 owns the separately authorized environment-specific retention schedule;
moderator assignment, deployment evidence, and Production remain controlled
operational steps. P-055/A-063 define the template-send database/domain
foundation; A-064 defines its reachable Flutter experience and A-065 owns its
retention schedule. P-056/A-066 define the chronological friends-only public-
template feed. A-067 closes the Android Dev/Production environment-separation
contract while leaving staging, iOS schemes, signing, and distribution separately
deferred.

## Deferred but accepted direction

These items are part of the agreed direction but intentionally deferred:

- Local SQLite caching for offline-tolerant active-list usage.
- Presence, Broadcast Replay, and client-originated Broadcast.
- FCM and APNs push delivery; Firebase project creation requires separate explicit
  authorization.
- Notification preferences and physical cleanup.
- Moderator assignment and each environment's retention scheduling require
  separate controlled rollout steps; Production remains separately authorized.
- Production backend/environment creation under a separate explicit authorization.

## Open product decisions

| ID | Question |
| --- | --- |
| O-P04 | What support or administrator correction path, if any, exists for an immutable username, and what audit/authorization rules govern it? |
| O-P12 | What archive/delete/preferences controls, push-safe content, physical cleanup, and retention rules apply beyond accepted friend/list/assignment/mention/Public-Template-moderation notifications? |
| O-P14 | Which operations work offline, how are conflicts presented, and what promise can the UI make while disconnected? |
| O-P15 | What administrator deletion, appeal/compliance, moderation/legal retention beyond the accepted Public Template v1 contract, Storage cleanup, and compliance obligations extend the accepted account lifecycle? |
| O-P16 | When does avatar editing ship, where are avatar objects stored, and what validation, privacy, replacement, and deletion lifecycle applies? |
| O-P17 | Which additional locales beyond English and Portuguese ship first? |

## Open architecture and data decisions

| ID | Question |
| --- | --- |
| O-A01 | What exact feature folder depth, domain/use-case boundaries, and Riverpod code-generation conventions should be adopted as features arrive? |
| O-A02 | What notification-link and later non-Auth feature deep-link contracts extend the accepted authenticated shell? |
| O-A04 | Which atomic operations belong in PostgreSQL functions versus Edge Functions? |
| O-A05 | What offline transaction boundaries extend the accepted online list/template copy versioning model? |
| O-A07 | Which SQLite package, cache schema, mutation queue, tombstone, retry, and conflict algorithm should be used? |
| O-A08 | What physical identifiers, audit/soft-delete conventions, indexes, and constraint strategy should future stored aggregates use beyond the accepted read-through feed and template-send model? |
| O-A12 | What payload/localization contracts for later notification types and what push-token/delivery schema are needed? |
| O-A13 | Which Storage use cases exist and what object lifecycle/policies follow their parent records? |
| O-A14 | Which logging, analytics, crash reporting, privacy controls, and performance budgets are appropriate? |
| O-A15 | What extended automated environment will exercise hosted Realtime, Storage, and later cross-service integration beyond deterministic bounded-handshake/recovery and gateway-to-mounted-projection tests, the accepted local private-channel transport smoke test, and CI migration/database/RLS coverage? |

## Closed decision references

| ID | Resolution |
| --- | --- |
| O-A03 | Closed on 2026-07-29 by A-002, A-017, and A-067: explicit Android `dev`/`prod` flavors, isolated package identities, one additional `APP_ENV` compile-time value, native flavor/application-ID cross-check before Supabase initialization, exact Dev-project binding, distinct callback schemes, and fail-closed unconfigured Production; staging, iOS flavor/scheme work, signing, and distribution remain deferred. |
| O-A09 | Closed on 2026-07-18 by P-023, P-024, A-021, and A-022: one RPC-only normalized current relationship row, five check-constrained states, version-checked deterministic transitions, minimal caller-relative projections, and no detailed audit log. |
| O-A06 | Closed on 2026-07-21 by P-040 and A-040 through A-043: one exact private account topic, receive-only identity authorization, fixed opaque contract, transaction-local database fanout, bounded-handshake SDK reconnect with serialized unhealthy-subscription recovery, join/resume reconciliation, generation-safe single-flight coalescing, and explicit deferral of Replay/Presence/offline writes. |
| O-P01 | Closed on 2026-07-21 by P-041 and A-044: current-owner-only active-list transfer to an exact accepted unblocked member, explicit confirmation, atomic sole-owner swap, retained monotonic access lineages, unchanged capacity/content, one informational notification, and no offline or password-reauthentication path. |
| O-P02 | Closed on 2026-07-20 by P-034a and A-035a for the owner-only active-list item model: exact thousandths, stable units, duplicate names, integer order, completion attribution, versions, locking, and conflict handling. Assignment is separately resolved by P-048 and A-051 through A-053; detailed item audit/history and offline synchronization remain deferred. |
| O-P05 | Closed on 2026-07-20 by P-036, P-037, A-037, and A-038: friendship ending cancels pending invitations but preserves membership; blocking applies atomic symmetric separation without third-party removal authority; unblocking restores nothing. |
| O-P09 | Closed on 2026-07-23 by P-044 and P-046: CHF/EUR use two minor-unit digits; currency is mutable only while no expense exists and no settlement history has ever existed, and the first settlement locks it permanently. Expense totals remain bounded at 999999999 minor units with 200 current expenses per list. |
| O-P11 | Closed on 2026-07-23 by P-046 and A-049: external-bookkeeping direction, full/partial amount validation, immutable history, one-time reversal, historical identities, permissions, bounded history display, and deterministic non-minimum suggested payments are fixed. |
| O-A11 | Closed on 2026-07-23 by P-046 and A-049: balances include non-reversed settlement effects and the stable server output greedily matches largest debtors/creditors with UUID tie-breaks without promising mathematical minimality. |
| O-P10 | Closed on 2026-07-24 by P-047 and A-050: exact positive minor-unit custom shares use explicit share rows, exact conservation, zero-as-deselection, no persisted allocation mode, canonical Equal inference/conversion, existing-role-only historical retention, payload-safe versioned RPCs, and legacy non-equal overwrite protection. |
| O-P03 | Closed on 2026-07-25 by P-049, P-050, A-054, and A-056: one normalized versioned list-level note, explicit stable-profile-ID-only links, complete canonical token/server eligibility validation, deterministic rendering/editing, new-only notification deduplication, permanent privacy suppression, and no implicit text resolution. |
| O-A10 | Closed on 2026-07-25 by A-054: PostgreSQL owns authoritative full-token/link validation and stable-ID storage; Flutter owns explicit selection/insertion and lightweight rendering behind the repository boundary, with no username snapshot or token-range persistence. |
| O-P06 | Closed on 2026-07-25 by P-051, P-052, A-058, and A-059 for private-copy/null-category placement, no provenance/attribution, explicit owner publication, profile-only friend/nonfriend visibility, strict public allowlists, block symmetry, independent copy behavior, and accessible presentation. That delivery remained profile-only; P-056/A-066 later resolve the separate feed-v1 contract. |
| O-P13 | Closed on 2026-07-27 by P-053, P-054, and A-060 through A-062: exact-revision individual reporting with eight reasons and immutable allowlisted snapshots; reporter-only hiding; empty UUID-only moderator authorization; bounded review; dismiss, restriction-backed takedown, and non-publishing restore; safe actor-null owner outcomes; 24-month closed-evidence cleanup to nonidentifying tombstones; one separately controlled daily `postgres` retention schedule per authorized environment; no v1 withdrawal/unhide/attachment/appeal/automation; and separately controlled moderator/bootstrap, hosted-rollout, and QA steps. |
| O-P08 | Closed on 2026-07-27 by P-055 and A-063: five-state friend-only immutable offers; zero-to-200 items; no pending expiry; sender revoke; recipient accept/decline; one pending triple; payload-bound idempotency and stable repeated acceptance; independent private Uncategorized copies; relationship/block/source/moderation/account lifecycle; persistent Received/minimal Sent; one recipient-only notification; 180-day terminal retention; and privacy-safe export v11. |
| O-P07 | Closed on 2026-07-27 by P-056 and A-066 for feed v1: current accepted friends only; self/block/report/restriction exclusion; publication-time/UUID order; default-20 bounded keyset pages; no count or age cutoff; read-through storage; and refresh-based freshness without ranking, retention, notification, or new Realtime fan-out. |

## Partially resolved decision references

| ID | Resolution |
| --- | --- |
| O-P12 | Partially resolved on 2026-07-27 by P-025, P-026, P-038, P-048, P-050, P-054, P-055, A-052, A-056, A-060, A-061, and A-063 for friend/list/assignment/mention/Public-Template-moderation/template-send notification persistence, action state, badge, block/access-loss suppression, mark-read privacy, safe actor-null outcomes, and 180-day expiry/terminal cleanup. The narrowed question above remains open. |
| O-P15 | Partially resolved on 2026-07-27 by P-027, P-028, P-030a, P-031a, P-033a, P-039, P-041, P-042, P-046, P-048 through P-055, A-025, A-026, A-032a, A-036a, A-039, A-044, A-045, A-049, and A-052 through A-063 for schema-versioned export and self-service deletion including ownership transfer, private/public template ownership and copies, template-send snapshots/history, shared-list impact, current assignments/mentions, Split settlement history, Public Template evidence retention, deterministic cleanup, and cascades/anonymization. Administrator deletion, appeal/compliance, moderation/legal retention beyond v1, Storage cleanup, and broader compliance obligations remain open. |
| O-A02 | Partially resolved on 2026-07-20 by A-036a for the four-tab state-preserving authenticated shell and root notification overlay. Notification and later feature deep-link contracts remain open. |
| O-A04 | Partially resolved on 2026-07-27 by A-035a, A-037 through A-039, and A-045 through A-063: shared-list/access/note/notification/export/deletion-impact/private/public-template copy/report/moderation/template-send/Split/assignment operations belong in exact PostgreSQL RPCs. Placement for other future operations remains open. |
| O-A05 | Partially resolved on 2026-07-27 by A-035a, A-037, A-046, A-049 through A-051, A-054, A-055, A-058, A-060, and A-063 for online shared-list/template/Split/assignment/note/moderation/template-send versions, payload-bound retry idempotency, capacity serialization, deterministic locking, and stale-write handling. Offline behavior remains open. |
| O-A08 | Partially resolved on 2026-07-27 by A-035a, A-037, A-045, A-047, A-049, A-051, A-054, A-058, A-060, A-063, and A-066 for list/item/access/financial-participant/private/public-template/template-send/report-group/moderation-event/settlement/assignment/mention identifiers, timestamps, state constraints, retention indexes, and request ledgers. Feed v1 is read-through, with no feed identifier, stored row, audit/soft-delete convention, retention model, or speculative global index. The narrowed later stored-aggregate question remains open. |
| O-A12 | Partially resolved on 2026-07-27 by A-023, A-024, A-038, A-052, A-056, A-060, A-061, A-063, and A-064 for friend/list/assignment/mention/Public-Template-moderation/template-send notification storage, versioned listing/count, actor-null outcomes, exact action state, RPC contracts, and localized template-send routing. Later push payload/token/delivery details remain open. |
| O-P17 | Partially resolved on 2026-07-26 by P-016, P-048, P-049, P-053, and P-054: English and Portuguese are the required supported languages for assignment, General Note, reporting/moderation, and notification UI. Additional locales remain open. |

## Decision discipline

- Resolve the smallest decision set needed for the next vertical slice.
- Record decisions before or with implementation, especially when they affect
  authorization, money, copies, retention, or irreversible migrations.
- Prefer invariants enforced by database constraints and RLS over client-only
  assumptions.
- Treat schema migrations as forward-reviewed production artifacts even while
  operating only a development project.
- Do not close an open decision merely because one implementation happens to be
  convenient.
